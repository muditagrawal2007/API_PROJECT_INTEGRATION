This is the Project for the API integraton -> 
I had choosed the project Railway System to show the API integration -> 
Setup -> 
1) Clone the repository in the local IDE 
git clone https://github.com/muditagrawal2007/API_PROJECT_INTEGRATION.git
Features used are :
HTML , CSS , JS 

In this project I will fetch the data from the API all the details of the passengers and do the manipulation over it . like searching , adding , removing , also adding the filter to adjust according to me Use the filter , map , forEach and use the fetch and .then .catch to fetch the API and do the manipulation over the set Use the HTML , CSS to make the Static website and JS for the Dynamic behaviour 